version: '3'
services:
  app:
    container_name: aee_digital_api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=production
